# This config file will start a reva service that:
# - serves the permissions service using the demo driver
[shared]
jwt_secret = "changemeplease"

[grpc]
address = "{{grpc_address}}"

[grpc.services.permissions]
driver = "demo"

[grpc.services.permissions.drivers.ci]
